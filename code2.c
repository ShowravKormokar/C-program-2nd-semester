#include <stdio.h>

int main()
{
    int a,b;
    float a,b;
    a=10;
    b=6;

    printf("Addition: %f + %f = %f\n",a,b,a+b);
    printf("Subtraction: %f - %f = %f\n",a,b,a-b);
    printf ("Multiplication: %f * %f = %f\n",a,b,a*b);
    printf ("Division: %f / %f = %f\n",a,b,a/b);
    printf ("Remainder: %d %% %d = %d\n",a,b,a%b);
    printf ("i+c-2f/5 = %0.2f\n",i+c-2f*f/5);

    return 0;
}
